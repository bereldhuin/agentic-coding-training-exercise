# Server configuration
PORT=8000
HOST=0.0.0.0

# Database path (relative to server/python directory)
# Default: ../database/db.sqlite (shared with TypeScript server)
DATABASE_PATH=../database/db.sqlite

# Environment
ENVIRONMENT=development
